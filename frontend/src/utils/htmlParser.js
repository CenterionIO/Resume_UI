// HTML Parser utilities
export const normalize = (text) => {
  return text.replace(/\s+/g, ' ').trim();
};

export const parseJobDescriptionSections = (htmlBlock) => {
  // Implementation from your html_parser.py
  // ... (copy the parsing logic)
};

export const extractJobMetadata = (scrapedContent) => {
  // Implementation from your html_parser.py  
  // ... (copy the metadata extraction logic)
};